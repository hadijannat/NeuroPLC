[project]
name = "neuroplc-cortex"
version = "0.1.0"
description = "NeuroPLC Python Cortex"
requires-python = ">=3.10"

[project.scripts]
neuroplc-cortex = "agent.supervisor:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
